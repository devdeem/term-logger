/*
    Contacts:
    Discord : @deemdev
    Support Server : https://discord.gg/UdKSrxBXyd
 */

// Import necessary modules
const { Logger } = require("term-logger");

try {
  // Your code here:)
  Logger.debug("aaaaaaaa my balls itch");
} catch (e) {
  // When there is an error in the code it sends an error message
  return Logger.error(e);
}

// ALL LOGGER OPTIONS:

/**
Logger.error("message");
Logger.debug("message");
Logger.info("message");
Logger.warn("message");
Logger.critical("message");
Logger.ready("message");
Logger.command("message");
Logger.event("message");
Logger.database("message");
Logger.shard("message");
Logger.cluster("message");
Logger.lang("message");
 */
